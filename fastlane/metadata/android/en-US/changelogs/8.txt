• Router list: file routers into groups — Home, Office, a client — with headers and filter chips; a mesh node follows its primary's group. Pin a group to keep it on top.
• Lost link: when a router stops answering, every screen says so, with a reconnect count, and clears once it's back.
• Destructive actions need a 3-second hold with rising haptics, not a second tap.
• System: change the router's hostname in place.
• Speed test: routers without curl now honour the 10 s / 100 MB limit.