• Speedtest: plain HTTP, up to 100 MB or 10 s each way, dashboard polling paused while it runs — a small router's CPU no longer caps the number.

• Channel advice judges neighbours by their real width and signal, says when a scan only heard its own channel, and offers a survey with the radio off.

• Radio editor: TX power (showing what the driver really applies), beamforming, and only the widths the chip supports.

• Firewall › Defaults: flow offloading.
