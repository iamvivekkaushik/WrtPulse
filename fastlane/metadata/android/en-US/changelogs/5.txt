• IoT Wi-Fi on the dashboard: one tap makes a smart-home network your LAN can reach but that cannot reach your LAN.

• Two routers on the same address can both be saved, each with its own host key, no changed-key warning. Host keys and backups follow the entry when its address changes.

• Import and Save copy on the backup screen no longer crash.

• Pull down to refresh on most pages.

• swconfig VLANs show sockets as the case labels them. Two-chip routers can apply again.
