package_name("com.vivekkaushik.wrtpulse")

# Play Console service-account JSON, for the `beta` lane only. Never committed: CI passes it
# as PLAY_JSON_KEY; locally point this at a file outside the repo.
json_key_file(ENV["PLAY_JSON_KEY_FILE"]) if ENV["PLAY_JSON_KEY_FILE"]
